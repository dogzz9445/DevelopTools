<Page x:Class="DDT.Apps.DDTOrganizer.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DDT.Apps.DDTOrganizer.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginView">
    <Page.Resources>
        <local:UserConveter x:Key="UserConverter" />
    </Page.Resources>

    <Grid>
        <StackPanel Margin="24">
            <TextBlock Text="{Binding CurrentUser,
                            UpdateSourceTrigger=PropertyChanged, Mode=OneWay, Converter={StaticResource UserConverter}}"
                       FontSize="20"
                       Foreground="White" />
            <Button Content="랜덤이름"
                    Command="{Binding SetRandomUserNameCommand}" />
        </StackPanel>
    </Grid>
</Page>
